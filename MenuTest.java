package model;
import junit.framework.*;
public class MenuTest {

	@test
	public void testDishes() throws Exception
	{
		Menu m = new Menu(m);
		Dish d = new Dish();
		AssertEquals(m.setDishPrice(d, d.getPrice()));
		
	}
}
